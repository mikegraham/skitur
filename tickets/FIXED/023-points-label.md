# 023: "Points" Label Ambiguous
**Status: FIXED
**Priority:** P2

## Description
Change "Points" to "GPS points" or similar in stats panel so it's clear it's not a scoring thing.

## Fix
Changed `"Points"` to `"GPS points"` in the stats table in `index.html`.
